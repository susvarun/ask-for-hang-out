const documentHeight = document.documentElement.clientHeight;
const documentWidth = document.documentElement.clientWidth;
console.log(`Document height: ${documentHeight}px, Document width: ${documentWidth}px`);
